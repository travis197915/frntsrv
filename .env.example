# Node claims-corebackend — auth, users, dashboard BFF
# (also injectable at container runtime via -e / process.env → /config.js)
VITE_AUTH_API_BASE_URL=http://localhost:4000

# Django agentic-backend — builder, ingest, execute, agent-tools (hit directly)
VITE_AGENTIC_API_BASE_URL=http://localhost:8000

VITE_CLIENT_NAME=Optum
